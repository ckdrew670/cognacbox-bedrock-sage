context('heading', () => {
	before(() => {
		cy.visit('http://localhost:3000/pattern-library')
	});

	describe('heading', () => {
		it('should do something', () => {
			// start writing tests here
		});
	});
});
